# Reference spans — type these from the photographs

**Status: EMPTY — awaiting human transcription.**

These are objective 3a's CER ground truth. The harness refuses to score until
every block below is filled.

## How to fill this in

For each document, open the photo in `Downloads/sample proposals` and type the
named section **verbatim** — exactly what is on the page.

- **Copy the page, not the intent.** If the page misspells a word, type the
  misspelling. If it writes "Agritrack" where the title says "AgriTrack", type
  what that line actually says. The model is being scored against the page.
- **Include the heading itself** (e.g. `III. Solution`) and everything under it
  up to the next heading.
- Line breaks and indentation don't matter — whitespace is collapsed before
  scoring. Type it as one flowing block if that's easier.
- Punctuation and capitalisation **do** matter. Type them as written.
- If a character is genuinely illegible, type your best single-character guess
  rather than a marker like `[?]` — a marker scores as a real error against the
  model's correct read.

**Do not look at the model's output first.** The whole value of this reference
is that it was written blind. The run may already have executed; its
transcriptions stay unopened until this file is complete.

Sections were drawn by seed `20260905` — not chosen. See
`docs/superpowers/specs/2026-09-05-ocr-accuracy-design.md`.

---

## Agritrack.jpg — Writer A — `V. Objectives`

```text

```

## Mediqueue.jpg — Writer A — `III. Solution`

```text

```

## Sakayscan.jpg — Writer A — `I. General Information`

```text

```

## BalikBasura.jpg — Writer A — `III. Solution`

```text

```

## EskwelaKo.jpg — Writer A — `II. Problem`

```text

```

## Anilink.jpg — Writer B — `IV. Target Market`

```text

```

## AquaSense.jpg — Writer B — `C. Technical Proposal`

```text

```

## BarangayPass.jpg — Writer B — `Expected Outcomes`

```text

```

## GoldChain.jpg — Writer B — `3. Proposed Solution`

```text

```

## RxScan.jpg — Writer B — `2.) System Architecture`

```text

```
